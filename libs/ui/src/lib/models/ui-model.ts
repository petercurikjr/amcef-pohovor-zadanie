export enum UiInputFormatType {
  TEXT,
  NUMBER,
}

export enum UiInputInputType {
  INPUT,
  TEXTAREA,
}

export enum UiButtonType {
  BASIC,
  SMALL,
  AUTH,
  ICON,
}

export enum UiBasicButtonType {
  FILL,
  OUTLINE,
}

export enum UiButtonColor {
  PINK,
  BLACK,
}
